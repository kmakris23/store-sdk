Deprecated: Previously used to bootstrap the Store SDK JWT Auth plugin as a MU plugin.

The implementation now lives solely under wp-plugin/store-sdk-jwt-auth and is mounted as a normal plugin.
This folder can be removed once any legacy references are cleaned up. The bootstrap file is a no-op.
